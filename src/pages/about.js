import React from 'react';

export const About = React.createClass({
  render() {
    return (
      <div>
        This is a very basic project using React that you can use to bootstrap your own projects.
        Everything is already setup for dev, test and linting.
        This project is using ES2015 syntax with the help of Babel 6.
      </div>
    );
  },
});
